using UnityEngine;
using System.Collections;

public class BoxScript : MonoBehaviour
{

    // Use this for initialization
    void Start()
    {
        //originalY = transform.position.y;
    }


    float angle = 0;
    float originalY;

    // Update is called once per frame
    void Update()
    {
        //transform.Rotate(0, Time.deltaTime * 60, 0, Space.World);

        //angle += 100 * Time.deltaTime;

        //if (angle > 360) 
        //    angle -= 360;

        //Debug.Log(Mathf.Sin(angle * Mathf.PI / 180));

        //transform.position = new Vector3(transform.position.x, originalY + 0.5f * Mathf.Sin(angle * Mathf.PI / 180), transform.position.z);


        
    }
}
